<!DOCTYPE html>
<html>
<head>
  <title>Edit Entry</title>
  <link rel="stylesheet" href="/stylesheets/editBudget.css">
</head>
<body>
  <div class="container">
    <h1>Edit Entry</h1>
    <form action="/update" method="POST">
      <input type="hidden" name="id" value="<%= item._id %>">

      <label>Title:</label>
      <input type="text" name="title" value="<%= item.title %>" required>

      <label>Amount:</label>
      <input type="number" name="amount" value="<%= item.amount %>" required>

      <label>Type:</label>
      <select name="type">
        <option value="income" <%= item.type === "income" ? "selected" : "" %>>Income</option>
        <option value="expense" <%= item.type === "expense" ? "selected" : "" %>>Expense</option>
      </select>

      <button type="submit">Update</button>

      <button type="submit">Add</button>
      <a href="/" class="back-btn">Back</a>
    </form>
  </div>
</body>
</html>
